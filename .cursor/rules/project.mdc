---
description: 项目整体约定，所有对话都应遵循
alwaysApply: true
---

# 优质奶牛管理信息系统


## 前端样式约定

- 表格中的按钮，都是文字样式，并需要配备图标
- 表格中的按钮，特别是操作列，由于有图标，所以要稍大点空隙
- 表格中的按钮，特别是操作列的第一个，要考虑主题色
- 表格外的新增搜索等按钮，需要有图标，实心样式
- 其中，资源的新增按钮就叫新增，不要叫新增xx
- 列表页顶部不要放蓝色说明提示条（如 `NAlert` 说明块），优先保持页面简洁

## 列表页交互约定

- 表格默认使用前端分页，默认每页 10 条，可切换每页条数
- 筛选条件变化后，分页应自动回到第一页
- 列表页高度要尽量自然撑开，不要因为错误的 `flex-height` / 固定高度组合导致只剩表头和分页器
- 若需要滚动，优先页面整体滚动；不要轻易让小数据量的表格内部出现大块空白滚动区

## 基础信息模块约定

- 基础信息管理中的新增、编辑、删除，优先使用前端状态管理实现，当前统一使用 Pinia
- 删除操作必须有二次确认
- 存在关联关系的数据删除前要先做前端校验，并给出明确提示
- 资源数据结构继续按两级关系组织与展示
